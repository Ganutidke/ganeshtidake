
import DashboardClient from '@/components/admin/dashboard-client';

export default async function AdminDashboard() {
  // In a real application, you would fetch your data here and pass it to the client component.
  // For this design task, the data is hardcoded within the client component.
  return <DashboardClient />;
}
